server:
  port: 8081
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "media"
  sslmode: "disable"
  timezone: "Asia/Shanghai"

auth:
  jwt_secret: "your-jwt-secret"
  token_expiry: "24h" 